/// <summary>
/// enum used to decide how the map is currently being displayed
/// </summary>
public enum MapVisualizationMode
{
    Default = 0,
    Graphics = 1,
    Height = 2,
    Heat = 3,
    Moisture = 4,
}